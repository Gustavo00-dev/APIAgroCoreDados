apiVersion: v1
kind: Service

metadata:
  name: agrocoredados-service

spec:
  type: NodePort

  selector:
    app: agrocoredados

  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30007
